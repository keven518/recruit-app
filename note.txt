`npm run eject` 弹出配置文件，可以自定义配置webpack

router
1.cnpm i react-router-dom -S

import { BrowserRouter, Route, Link } from 'react-router-dom'

exact 就不包含app 7‘

// 开启数据库
1、mongod --config /usr/local/etc/mongod.conf
2、mongo

// 6-3
package.json
babel-plugin-import 按需加载

// 6-6
关于 return 问题

avatar-selector 如何把选择的图片传给bossinfo的
为什么要删除user.js中的39行
onClick={this.handleSubmit} = onClick={() => this.handleSubmit()}

// 9-3
redux 数据不能在componentDidMount()中显示